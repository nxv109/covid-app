import styled from "styled-components";

export const Input = styled.input`
  background: transparent;
  color: #fff;
  padding: 0.5rem;
  border: 1px solid #5cb3dd;
  outline: none;
  border-radius: 5px;

  &::placeholder {
    color: #5cb3dd;
  }
`;
